# Scholarship Calculator

## Requirements
- PHP 8.x
- MySQL
- Composer

## Setup
1. Clone the repo
2. Run `composer install`
3. Import database: run the SQL in `database.sql`
4. Update `db.php` with your database credentials
5. Start server: `php -S localhost:8000`
6. Open `http://localhost:8000`